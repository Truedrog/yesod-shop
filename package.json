{
  "name": "yesod-shop",
  "version": "1.0.0",
  "scripts": {
    "heroku-prebuild": "cd frontend && npm install && npm run build"
  },
  "cacheDirectories": ["frontend/node_modules"],
  "engines": {
    "yarn": "1.19.1"
  }
}
